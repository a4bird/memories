mutation photoPutPreSignedUrl(
  $albumId: Int!
  $filename: String!
  $filetype: String!
) {
  photoPutPreSignedUrl(
    albumId: $albumId
    filename: $filename
    filetype: $filetype
  ) {
    signedRequest
    url
  }
}
